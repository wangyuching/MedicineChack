fitness need to up.
loss need to down.

epoch:read hole book once.
batch:read ? page, note once.
lteration:note time when read book.
have 160 pic, batch=16, 10times lteration a epoch. 

CHACK results.png/.csv 's Validation Loss:
    if down > add epoch,
       up > stop.